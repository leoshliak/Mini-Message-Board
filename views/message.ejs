<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://kit.fontawesome.com/ce1faa7116.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/styles.css">
    <title>Message Details</title>
</head>
<body>
    <%- include('header', {title: 'Message Details'})  %> 
    <main>
        <div class="message-detail">
            <% if (message) { %>
                <div class="detail-con">
                    <h2 class="detail-user"><%= message.user %></h2>
                    <p class="detail-text"> <%= message.text %></p>
                    <p class="d-message-date"><%= message.added %></p>
                </div>
                <div class="message-actions">
                    <a href="/" class="btn-back">Back to Messages</a>
                </div>
            <% } else { %>
                <p>Message not found</p>
                <div class="message-actions">
                    <a href="/" class="btn-back">Back to Messages</a>
                </div>
            <% } %>
        </div>
    </main>
</body>
</html>